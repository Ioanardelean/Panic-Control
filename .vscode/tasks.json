{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "server:build:dev",
      "windows": {
        "command": "npm.cmd"
      },
      "linux": {
        "command": "npm"
      },
      "args": ["run", "server:build:dev"],
      "options": {
        "cwd": "${workspaceRoot}"
      }
    }
  ]
}
